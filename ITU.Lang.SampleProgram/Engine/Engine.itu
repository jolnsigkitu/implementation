/*
type ProducerSignal<KeyboardEventArgs> = any;
type KeyboardEventArgs = any;
*/

type Engine = {
    /*
    constructor() {
        print("I am become engine");
    }
    */

    init(signal: ProducerSignal<KeyboardEventArgs>) {
        signal.ForEach((evt: KeyboardEventArgs) {
            print(evt.Key);
        });
    }

    getState() => "foobar";
};
