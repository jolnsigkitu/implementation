type User = {};

operator (a: int (+) b: int) => a + b;
operator ((!) a: boolean) => {
    if a == true {
        return false;
    }
    return true;
};
operator (a: int (++)) => a + 1;
operator (a: int (--)) => a - 1;

let b: a = 32;
