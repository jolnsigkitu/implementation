// Case 1: loops
let i: int; // What is the undeclared variable? Is this obvious? Could it be un-usable when still unassigned?

for(i = 0; i<5; i++) {

}

print(i) // We need to verify the assignment somehow

// Case 2: try-catch

let connection: Connection?;

try {
    connection = createConnection();
}
catch(Exception ex) {
    //...
}
finally {
    connection?.close();
}
