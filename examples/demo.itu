const createMultiplier = (fac: int) => (x: int) => fac * x;

const mul2 = createMultiplier(2);

const res = mul2(3);

const tickTock = (i: int) => (i % 2 == 0) ? "Tick": "Tock";

tickTock(0);
tickTock(1);
